@model IEnumerable<MvcMovieProject.Models.MovieRole>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Character)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Actor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Character)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Actor.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Genre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MovieRoleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MovieRoleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MovieRoleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
